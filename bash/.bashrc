# ~/.bashrc
[ -f ~/.shell_bash ] && source ~/.shell_bash
[ -f ~/.shell_shared ] && source ~/.shell_shared
[ -f ~/.fzf.bash ] && source ~/.fzf.bash
[[ -r "/opt/homebrew/etc/profile.d/bash_completion.sh" ]] && . "/opt/homebrew/etc/profile.d/bash_completion.sh"
